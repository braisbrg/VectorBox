<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <title>VectorBox</title>
  <desc>The three trident signals. Middle bar in the ACID accent, hardcoded because a favicon renders outside the document and cannot read the theme, so it always shows the acid default.</desc>
  <rect width="32" height="32" fill="#000000"/>
  <rect x="7"  y="9" width="4" height="14" fill="#eeeeee"/>
  <rect x="14" y="5" width="4" height="18" fill="#f7e800"/>
  <rect x="21" y="9" width="4" height="14" fill="#eeeeee"/>
</svg>
