fastapi[all]==0.141.1
sqlalchemy==2.0.51
psycopg[binary]==3.3.4
alembic==1.19.0
pandas==2.3.3
sentence-transformers>=5.0.0
scikit-learn==1.9.0
qdrant-client==1.19.0
redis==4.6.0
orjson>=3.11.9
pillow>=12.3.0

PyJWT==2.13.0
cryptography==50.0.0
pydantic[email]==2.13.4
pydantic-settings==2.14.2  # GHSA-4xgf-cpjx-pc3j
pytest==9.0.3
pytest-asyncio==1.3.0
slowapi==0.1.9
asyncpg==0.31.0

groq==0.37.1
openai==2.37.0
instructor==1.14.5

tqdm==4.67.3
pip-audit==2.10.0
feedparser==6.0.12
beautifulsoup4==4.14.3


apscheduler==3.11.2
curl_cffi==0.15.0

# Observability (OpenTelemetry)
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
opentelemetry-instrumentation-fastapi==0.65b0
opentelemetry-instrumentation-sqlalchemy==0.65b0
opentelemetry-instrumentation-redis==0.65b0
opentelemetry-exporter-otlp-proto-grpc==1.44.0

# Build & Test Tools
wheel>=0.46.2          # GHSA-8rrh-rw8j-w5fx
playwright>=1.62.0     # For E2E tests inside container

# ── Suelos de seguridad para dependencias TRANSITIVAS ──────────────────────
# Ninguna de éstas la usamos directamente: las arrastran fastapi, aiohttp,
# transformers y compañía. Se declaran aquí sólo para forzar la versión mínima
# sin vulnerabilidades, que es la forma estándar de arreglar un transitivo sin
# esperar a que el paquete de arriba se actualice.
#
# Salieron el 2026-09-03, cuando se descubrió que `pip-audit --strict` abortaba
# en `torch+cpu` y la puerta llevaba meses pasando sin auditar NADA (84 avisos
# reales en 18 paquetes). Ver la clase C12 de docs/AUDIT_PLAYBOOK.md.
aiohttp>=3.13.4          # 24 avisos
click>=8.3.3
h2>=4.4.1
idna>=3.15
mako>=1.3.12
msgpack>=1.2.1
pygments>=2.20.0
python-dotenv>=1.2.2
python-multipart>=0.0.26 # 5 avisos
requests>=2.33.0
soupsieve>=2.8.4
urllib3>=2.7.0
pip>=26.2                # PYSEC-2026-3721
# starlette la fija fastapi, que sólo pide `>=0.46.0` — sin tope superior, así
# que la 1.x cabe. Son 5 avisos y ninguno tiene arreglo por debajo de la mayor.
starlette>=1.3.1         # PYSEC-2026-161/248/249/2280/2281
# transformers NO se sube, y no por pereza: se intentó `>=5.10.0` el 2026-09-03 y
# la resolución arrastró `sentence-transformers` 5->6 y `torch` 2.10->2.14. Eso no
# es un parche de seguridad, es reescribir el stack de ML — y aquí cambiar el
# encoder obliga a re-embeber las 21.425 películas y re-clusterizar a todos los
# usuarios (ver CLAUDE.md). Sus 6 avisos (2 sin arreglo) quedan aceptados a
# sabiendas hasta que ese trabajo se haga a propósito y con su medición delante.

# Security note: diskcache 5.6.3 has GHSA-w8v5-vhqr-4h but no 5.6.4 exists yet.
#
# NO SE SUBEN, y por qué — las dos piden una MAYOR y necesitan su propia prueba:
#   starlette 0.50.0 -> 1.x   (5 avisos) la fija fastapi; subirla a ciegas puede
#                             romper el ASGI de toda la app.
#   transformers 4.57.6 -> 5.x (4 avisos, +2 sin arreglo) toca el encoder de
#                             embeddings; un cambio ahí reescribe el espacio
#                             vectorial entero.
